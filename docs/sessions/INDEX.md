# Conversations Index

| File | Phiên | Nội dung tóm tắt | Trạng thái |
|------|-------|------------------|------------|
| [phien1-acc-lxn.md](phien1-acc-lxn.md) | Phiên 1 | Audit kiến trúc toàn bộ. Fix PII leak: `/api/tickets` GET + `/api/sse/queue` stream tên+SĐT công dân tới anonymous. Thêm `api-auth.ts` optional auth, lọc field theo role ở tickets route + sse-broker. Findings còn mở: SQLite thiếu `busy_timeout`, `queue-service.ts` chưa có test, cookie `secure` flag không nhất quán. | Fix được merge thực tế ở phiên 2 (2026-08-06); còn 4 việc pending |
| [phien2-acc-skde.md](phien2-acc-skde.md) | Phiên 2 | Phát hiện fix PII phiên 1 chỉ tồn tại `authenticateOptional()` trong api-auth.ts nhưng không được gọi; tickets GET + sse-broker + sse/queue route vẫn leak `customerName`/`phone`. Author 3 file fix (`tickets/route.ts` + `redactTicketsForRole`, `sse-broker.ts` + `redactForRole`/`QueueClient.role`, `sse/queue/route.ts` truyền role). `broadcastDisplayCallLocal` giữ nguyên (cố ý). Snapshot ở `file changed/`. | **ĐÃ MERGE vào repo 2026-08-06**: 4 file + `tsconfig.json` exclude `conversations` (snapshot trùng `declare global` phá type-check). type-check pass; lint pass (trừ 5 lỗi pre-existing `get-ticket/page.tsx`). Todo: thêm test e2e cho anonymous GET |
| [phien3-acc-48c1.md](phien3-acc-48c1.md) | Phiên 3 | Chốt ưu tiên: e2e verify PII trước, unit test `queue-service.ts` sau, `/api/settings`+P2 cuối. Thêm 3 bước vào `scratch/e2e-test.mjs`: 3b Anonymous GET `/api/tickets` không thấy PII, 4b STAFF vẫn thấy PII (chống over-redact), 4c Anonymous SSE 3s không lọt PII. Giả định: response mảng/`.tickets`/`.data`, SSE `?serviceId=`. | **ĐÃ MERGE 2026-08-06** — `scratch/e2e-test.mjs` khớp 100% snapshot, `node --check` pass. Chưa chạy thử (cần `npm run dev` + `node scratch/e2e-test.mjs`). |
| [phien6-acc-opencode.md](phien6-acc-opencode.md) | Phiên 6 | Hoàn tất 5 task còn lại: cookie `secure` flag thống nhất (`isSecureCookie()`), sạch lint 100%, CSP header, CI/CD GitHub Actions, chốt DB production = SQLite. **8/8 task HANDOFF xong.** | Hoàn thành 2026-08-07. Snapshot ở `file changed/`. Rút kinh nghiệm: không dùng `git add -A` khi chạy phiên song song. |
